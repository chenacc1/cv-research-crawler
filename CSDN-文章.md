# 一款让CV论文追踪效率翻10倍的开源工具，我后悔没早点做出来

## 你是不是也这样？

每天打开arxiv，cs.CV分类下又多了200篇新论文。

快速扫一遍标题，点开几篇看起来相关的，读摘要、判断价值、决定是否精读。然后还有GitHub上的开源项目要看，"最近又有什么新仓库冒出来了？那个NeRF的代码更新了吗？"

一整套操作下来，一个小时没了。更让人崩溃的是——三天后你隐约记得有篇论文讲了你关心的问题，但就是想不起来是哪篇、在哪个平台看到的。

**如果你也是这样，那这个工具就是为你准备的。**

---

## 它是什么？

**CV Research Crawler** — 一个帮你自动追踪CV领域最新论文和GitHub项目的本地工具。

把它跑在你自己的电脑上，它会：
- 每隔几小时自动扫描arxiv最新论文和GitHub热门仓库
- 用你本地的AI大模型给每篇论文、每个项目写中英文总结
- 让你用关键词精准控制"什么内容值得存"
- 生成每日/每周的Markdown科研简报

全程**不需要联网、不需要注册、数据完全在你本地**。

> GitHub仓库：https://github.com/chenacc1/cv-research-crawler

---

## 它解决什么痛点？

### 痛点一：信息过载，看不完

arxiv每天新增上百篇论文。你不可能全看，但你又怕错过重要的。

**解决方案：AI帮你读。** 工具爬下论文后，自动调用你本地的Ollama大模型，用中文总结每篇论文的核心贡献、方法和创新点。3-5句话，一眼就知道这篇论文值不值得深读。

![Dashboard](screenshots/dashboard.png)

### 痛点二：搜索太宽泛，筛选太粗糙

arxiv的搜索只能按分类+关键词。但你真正关心的可能是更细的方向——比如"基于扩散模型的医学图像分割"。

**解决方案：LLM关键词扩展。** 你输入一个大的研究方向如"3D视觉"，AI自动帮你列出"NeRF、Gaussian Splatting、点云、深度估计、SLAM..."等十几个细分关键词。你勾选需要的，爬虫就只抓匹配的内容。

![Keywords](screenshots/keywords.png)

### 痛点三：看完就忘，找不到

"我记得有篇论文讲了视觉Transformer的轻量化，但我忘了名字..."

**解决方案：自定义标签。** 看到感兴趣的论文随手打上"轻量化"、"必读"等标签，之后按标签秒筛选。标签带颜色，一目了然。

### 痛点四：每天手动刷，效率低

每天打开arxiv、GitHub、Papers With Code...机械重复。

**解决方案：全自动定时爬取。** 工具内置定时任务，arxiv每6小时、GitHub每2小时自动扫描。你只管打开网页看结果就行。不想等？一键"立即触发"即可手动爬取。

![Crawl Status](screenshots/crawls.png)

### 痛点五：英文论文读着累

"摘要看懂了，但要跟别人讲这篇论文做了什么，还得自己翻译组织语言。"

**解决方案：自动生成中英双语摘要。** 中文总结方便快速理解，英文总结方便论文写作时引用。而且可以一键切换整个界面语言。

---

## 用什么AI模型？

你只需要在电脑上安装 [Ollama](https://ollama.com)（一个本地大模型运行工具），然后下载任意模型：

```bash
ollama pull qwen3.5    # 中文能力强
# 或者
ollama pull gemma4     # 速度快
```

工具会自动调用Ollama生成摘要和扩展关键词。不需要API Key，不花一分钱，完全本地运行。

---

## 核心功能一览

| 功能 | 说明 |
|------|------|
| 自动爬取 | arxiv 5个CV分类 + GitHub 9个CV主题，定时自动 |
| AI摘要 | 中英双语，本地大模型生成，免费无限制 |
| 关键词筛选 | 输入方向 → AI扩展细分词 → 勾选 → 精准爬取 |
| 一键中文 | 全界面中英切换，标题按钮文字全变 |
| 定时报告 | 每晚10点日报 + 每周一上午10点周报 |
| 标签管理 | 16色调色板，论文/仓库均可打标签筛选 |
| 全文搜索 | 标题+摘要FTS5全文检索，支持多维度组合筛选 |
| 本地优先 | SQLite零配置，数据在你手里，断网也能用 |

![Papers](screenshots/papers.png)

---

## 五分钟跑起来

你的电脑需要：Python 3.12+、Node.js 18+、Ollama（可选）

```bash
# 1. 下载
git clone https://github.com/chenacc1/cv-research-crawler.git

# 2. 启动后端
cd backend
pip install -r requirements.txt
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# 3. 启动前端（新终端）
cd frontend
npm install && npm run dev

# 4. 打开浏览器 http://localhost:5173
```

从下载到看到论文数据，**不超过10分钟**。

---

## 写在最后

这个工具本质上是一个"外脑"——帮你把CV领域的论文和项目**自动收集、AI理解、分类存储、随时检索**。你节省下来的时间，可以用来真正做研究、写论文、跑实验。

它不是为了替代你的判断力，而是帮你**过滤噪音、放大信号**。

如果对你有用，欢迎[Star](https://github.com/chenacc1/cv-research-crawler)和分享给实验室同学。

---

**标签：** `论文追踪` `AI工具` `计算机视觉` `开源` `科研效率` `Ollama` `FastAPI`
